title: Start Panel
# preset: pages
unlisted: true

tabs:

  work:
    label: Work
    
    sections:
      work:
        headline: Work
        type: pages
        parent: site.find("work")
        layout: list
        info: "{{ page.position }}"
        template: default

  news:
    label: News

    sections:
      news:
        headline: News
        type: pages
        parent: site.find("news")
        layout: list
        info: "{{ page.position }}"
        template: news

  creatives:
    label: Creatives

    sections:
      creatives:
        headline: Creatives
        type: pages
        parent: site.find("creatives")
        layout: list
        info: "{{ page.position }}"
        template: default
